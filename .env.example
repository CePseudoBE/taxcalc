# Back-Taxes - Environment Variables
# Copier ce fichier vers .env et ajuster les valeurs

# ============================================
# PostgreSQL
# ============================================
POSTGRES_DB=backtaxes
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here

# ============================================
# Backend (Spring Boot)
# ============================================
BACKEND_PORT=8080

# ============================================
# Frontend (Nuxt)
# ============================================
FRONTEND_PORT=3000

# Session secret (minimum 32 caractères!)
# Générer avec: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-minimum-32-characters

# ============================================
# Production URLs (optionnel)
# ============================================
# Si vous utilisez un reverse proxy (nginx, traefik)
# PUBLIC_URL=https://backtaxes.example.com
# API_URL=https://api.backtaxes.example.com
